<%= form_with model: @movie, local: true, html: {class: 'form-horizontal'} do |form| %>
  <% if @movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movie.errors.count, "error") %> prohibited this @movie from being saved:</h2>

      <ul>
      <% @movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :user_id, :value => current_user.id %>

  <div class="form-group">
    <%= form.label :title, 'Название', class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= form.text_field :title, class: 'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <%= form.label :category, 'Категория', class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
    <%= form.collection_select :category_ids, Category.all, :id, :title, { label: "Categories" }, { multiple: true } %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= form.submit 'Сохранить', class: 'btn btn-success' %>
    </div>
  </div>
<% end %>
